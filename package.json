{
  "name": "web-pull",
  "version": "1.0.0",
  "description": "一键采集任意网页为 Markdown，支持代码块、公式、Mermaid、图片自动提取",
  "type": "module",
  "scripts": {
    "dev": "wxt dev",
    "build": "wxt build",
    "fix-paths": "node scripts/fix-paths.js",
    "zip": "npm run build && npm run fix-paths && wxt zip",
    "package": "npm run zip"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.5",
    "jsdom": "^29.0.2",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "wxt": "^0.18.0"
  }
}
